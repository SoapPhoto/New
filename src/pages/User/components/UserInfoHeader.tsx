import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  padding: 0 32px;
`;

const UserInfoHeader = () => {
  return <Wrapper>sdfsf</Wrapper>;
};

export default UserInfoHeader;
